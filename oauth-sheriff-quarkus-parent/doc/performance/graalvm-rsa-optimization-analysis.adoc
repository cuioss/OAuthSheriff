= GraalVM Cryptographic Performance Analysis
:toc: left
:toclevels: 3
:toc-title: Table of Contents
:sectnums:
:source-highlighter: highlight.js

== Executive Summary

**Analysis Date**: January 2026 +
**Primary Finding**: RSA signature verification performs excellently at 68us, accounting for only 0.1% of CPU samples.

== RSA Performance

=== JMH Benchmark Results

[cols="2,2,2", options="header"]
|===
| Operation | Time | Notes

| RSA-256 Signature Verification | 68us | Default JDK provider
| Full JWT Parsing + Verification | 77us | Includes JSON parsing
| Token Cache Lookup | 0.1us | Lock-free concurrent cache
|===

=== JFR CPU Profiling

48,027 execution samples under load (top-of-stack):

[cols="2,1,1", options="header"]
|===
| Component | Samples | Percentage

| TLS Encryption (GHASH, AES, CounterMode) | 8,970 | **18.7%**
| Network I/O (Socket read/write) | 4,369 | 9.1%
| JVM/GC Overhead | ~3,000 | 6.3%
| Vert.x/Netty Buffer Operations | 1,656 | 3.5%
| CUI Sheriff OAuth Library | ~150 | **0.3%**
| BigInteger (RSA) | 46 | **0.1%**
|===

== GraalVM Native Image Characteristics

* **No JIT Compilation**: Static AOT compilation
* **Predictable Performance**: No warmup period, consistent latency
* BigInteger operations perform well; default JDK RSA provider achieves 68us

=== GraalVM 25 / Mandrel jdk-25 Improvements

* **WP-SCCP**: Whole-Program Sparse Conditional Constant Propagation (default)
* **ML Profile Inference**: 1-3% performance boost
* **JCMD Support**: Runtime diagnostics via jcmd
* **Vector API**: Initial optimization support

== Algorithm Comparison

[cols="2,2,2,3", options="header"]
|===
| Algorithm | Type | Measured Time | Assessment

| RS256 | RSA 2048-bit | 68us | Current default, excellent
| ES256 | ECDSA P-256 | ~20-40us (est.) | Faster, marginal benefit
| Ed25519 | EdDSA Curve25519 | ~10-20us (est.) | Fastest, smaller signatures
|===

With RSA at 68us (3.5% of 1.91ms total latency), algorithm migration provides marginal improvement. Decision should be driven by standards compliance, key management, and token size rather than performance.

== JCA Provider Landscape

* **Default JDK** (Recommended): 68us RSA, full native image support, automatic updates
* **BouncyCastle**: Only if specific algorithms needed; requires reflection configuration
* **Conscrypt**: Not suitable for GraalVM native image (BoringSSL dependencies)
* **Jipher**: No longer bundled with GraalVM 25; consider only for FIPS compliance

== Performance Monitoring

[source,bash]
----
# Build with monitoring support
native-image --enable-monitoring=jcmd,jfr,heapdump YourApp

# Runtime JFR recording
jcmd <pid> JFR.start duration=60s filename=profile.jfr
jcmd <pid> JFR.dump filename=profile.jfr
----

Key metrics: RSA verification time (~68us), BigInteger CPU percentage (~0.1%), TLS overhead (~18.7%).
